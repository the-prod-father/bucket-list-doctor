{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[bucket-list-doctor: resuming from latest handoff]'; awk '/^## /{c++} c<2{print}' \"${CLAUDE_PROJECT_DIR:-.}/docs/sessions/CURRENT_CONTEXT.md\" 2>/dev/null",
            "timeout": 10,
            "statusMessage": "Loading session handoff"
          }
        ]
      }
    ]
  }
}
